# Minimum viable RAHP example

You do not need the entire RAHP corpus to start. A useful first assessment can contain one scope, a small affected-party set, a handful of risk hypotheses, only the controls necessary to reason about treatment, hard-stop guardrails where required, and actionable recommendations.

Use `review.yaml` as a lightweight working record. It is an adoption example, not part of the DTG canonical instance.
